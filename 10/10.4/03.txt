Подвиг 3. Имеется следующий фрагмент программы:

method = "POST"

match method:
    case "post" | "POST":
        print("POST-запрос")
    case "get" | "GET":
        print("GET-запрос")
    case _:
        print("Неподдерживаемый тип запроса")
Что будет выведено на экран при его выполнении?

---------------------------------------------------------------

POST-запрос      +

Будет синтаксическая ошибка из-за использования символа '|'

GET-запрос

Неподдерживаемый тип запроса