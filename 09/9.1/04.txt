Подвиг 4. Пусть имеется некоторый генератор gen, который возвращает числа.
Отметьте команды, которые можно выполнять с этим генератором.

---------------------

len(gen)

sum(gen)       +

tuple(gen)     +

gen[0]

max(gen)       +

min(gen)       +

list(gen)      +

set(gen)       +