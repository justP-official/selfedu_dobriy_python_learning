Подвиг 2. С помощью каких команд можно устанавливать сторонние пакеты?


pip install Django==2.1.2          +

pip install Django          +

pip install Django=2.1.2

pip -r requirements.txt

pip install -r requirements.txt          +