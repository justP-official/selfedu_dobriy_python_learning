Подвиг 4. Что будет записано в файл out.txt при выполнении следующего фрагмента программы:

try:
    with open("out.txt", "w") as file:
        file.write("Hello")
        file.write("Hello")
except:
    print("Ошибка при работе с файлом")

-------------------------------------------------------

будет записана только одна строчка "Hello"

две строчки "Hello" одна за другой в строчку через пробел

две строчки "Hello" одна под другой (в столбик)

две строчки "Hello" одна за другой (в строчку)            +